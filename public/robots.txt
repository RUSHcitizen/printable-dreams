User-agent: *
Allow: /

# TODO: add "Sitemap: https://<real-domain>/sitemap-index.xml" once the
# production domain is finalized — see README "Domain configuration" for
# the exact two steps (PRODUCTION_URL + @astrojs/sitemap).
